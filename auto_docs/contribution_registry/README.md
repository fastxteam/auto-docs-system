# 贡献台账建议

`release.toml` 只负责版本快照和文档点检。

如果后续需要做积分或贡献统计，建议单独维护在这个目录下，按周期拆分，例如：

```text
auto_docs/contribution_registry/
  2026Q2.toml
  2026Q3.toml
```

建议按“人员 + 周期 + 贡献事项”管理，而不是按版本管理：

- 一个人在一个周期内可以关联多个模块
- 每条记录保留贡献类型、说明、证据、审批状态
- 发布版本只做结果展示，不承担积分审批和归属判断

后续如果要自动汇总，可以再新增单独脚本扫描这个目录并生成贡献统计页。
